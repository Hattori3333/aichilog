document.getElementById("newButton").addEventListener("click", function() {
    // ここに新規ボタンが押されたときの処理を記述します
    console.log("新規ボタンが押されました");
    // 例えば、新しい何かを作成する関数を呼び出したり、特定のアクションを実行したりできます
  });
  
  // 高評価ボタンがクリックされたときの処理
  document.getElementById("likeButton").addEventListener("click", function() {
    // ここに高評価ボタンが押されたときの処理を記述します
    console.log("高評価ボタンが押されました");
    // 例えば、いいねのカウントを増やすなどの処理を実行できます
  });
  
  // 低評価ボタンがクリックされたときの処理
  document.getElementById("dislikeButton").addEventListener("click", function() {
    // ここに低評価ボタンが押されたときの処理を記述します
    console.log("低評価ボタンが押されました");
    // 例えば、いいねのカウントを減らすなどの処理を実行できます
  });